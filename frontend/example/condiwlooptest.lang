pp co.lang.int = 15;
(pp > 10 && pp < 14).do({
    co.nop;
}).otherwise( pp == 11 ).loop({
    co.nop;
}).otherwise(pp == 15).do({
    co.nop;
    co.out.print(pp);
}).otherwise.loop({
    co.nop;
    co.nop;
    co.out.print("in");
});
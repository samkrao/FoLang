// frontier.unit.fol
_ co.lang.unit = {
    run()->(co.lang.int) = {
        seed co.lang.int = 1;

        double(n co.lang.int)->(co.lang.int) = {
            this.return n * 2;
        }

        scale := 3;

        this.return double(seed) * scale;
    }
}

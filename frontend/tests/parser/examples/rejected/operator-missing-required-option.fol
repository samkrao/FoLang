Vector co.lang.unit = {
    @co.dap.operator(
        symbol="<+>",
        mode=define,
        fixity=infix,
        associativity=left,
        arity=binary
    )
    combine(left Vector, right Vector)->(Vector) = {
        this.return left;
    }
}

Vector co.lang.unit = {
    @co.dap.operator(
        symbol="<+>",
        mode=define,
        fixity=ternary,
        precedence=65,
        associativity=left,
        arity=binary
    )
    combine(left Vector, right Vector)->(Vector) = {
        this.return left;
    }
}

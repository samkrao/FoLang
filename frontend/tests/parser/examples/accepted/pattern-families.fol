literalPattern(0) => 0;
bindingPattern(value).where(value > 0) => value;
constructorPattern(Some(value)) => value;
recordPattern(Employee{id: value, name: _}) => value;
tuplePattern((left, right)) => left;
let captured(value) = value;

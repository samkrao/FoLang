_ co.lang.unit = {
    // @@new/@@init are declaration spellings only; construction is invoked
    // through the ordinary member names.
    build()->() = {
        a := Employee.new(co.lang.int, co.lang.string);
        c := Employee.new(co.lang.int, co.lang.string).init(1, "Rao");
        k := LinkedList.new(co.lang.int);
        actualList := k.init();
    }
}

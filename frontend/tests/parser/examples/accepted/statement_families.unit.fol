_ co.lang.unit = {
    holder()->() = {
        typed co.lang.int = 1, other co.lang.int = 2;
        inferred := 3, reusable ?= 4;
        (left co.lang.int, right co.lang.int);
        left, right = right, left;
        emptyCall();
        ;
        label: { localValue := 1; }
        {
            nested := 2;
            nested
        }
    }
}

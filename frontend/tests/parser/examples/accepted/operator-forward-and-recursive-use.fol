Vector co.lang.unit = {
    use(left Vector, right Vector)->(Vector) = {
        this.return left + right;
    }

    @co.dap.operator(symbol='+', mode=overload)
    add(left Vector, right Vector)->(Vector) = {
        combined := left + right;
        this.return combined;
    }
}

// Employee.fol
_ co.class = {
    Address co.struct = { ... } // ❌
}

// EmployeeModule.fol
_ co.module = {
    Config co.struct = { ... } // ❌
}

process()->() = {
    State co.enum = { Ready, Done } // ❌ named type declaration
}

// EmployeeContract.fol
_ co.signature = {
    Employee co.struct; // ❌
}

// EmployeeApi.fol
_ co.interface = {
    Result co.struct; // ❌
}

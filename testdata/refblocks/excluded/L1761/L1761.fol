
Option(T) co.type = co.variants(Some(T), None);

f(Some(x)) => { this => x + 1; }
f(None)  => { this => 0; }

// desugars to:
f(v Option(co.int))->(co.int) = {
    this => v.match()
        .case(x: Some(x) => x + 1)
        .case(_: None => 0);
}


Option(T) co.lang.type = co.lang.variants(Some(T), None);

f(Some(x)) => { this => x + 1; }
f(None)  => { this => 0; }

// desugars to:
f(v Option(co.lang.int))->(co.lang.int) = {
    this => v.match()
        .case(x: Some(x) => x + 1)
        .case(_: None => 0);
}

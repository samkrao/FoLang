@co.dap.onEffect(
    co.NetworkError={
        handlers=[LogRetryHandler],
        resolution=retry,
        retry={
            max_attempts=3,
            on_exhausted=return_with_error
        }
    }
)
sendRequest();

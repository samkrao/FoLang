_ co.unit = {
        IntAdder co.type = (co.int)->(co.int);

        adder()->(IntAdder) ={
            sum co.int = 0;
            this => (x co.int)->(co.int){
                sum += x;
                this => sum;
            };
        }
}

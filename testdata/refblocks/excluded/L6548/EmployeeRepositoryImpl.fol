// EmployeeRepositoryImpl.fol
_ co.module->(
    signature=Repository,
    matches=Repository
) = {
    Entity co.type = hr.employee.Employee;

    current Entity = ...;
    find(id co.int)->(Entity) = { ... }
}

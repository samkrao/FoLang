_ co.lang.unit = {
        IntAdder co.lang.type = (co.lang.int)->(co.lang.int);

        adder()->(IntAdder) ={
            sum co.lang.int = 0;
            this => (x co.lang.int)->(co.lang.int){
                sum += x;
                this => sum;
            };
        }
}

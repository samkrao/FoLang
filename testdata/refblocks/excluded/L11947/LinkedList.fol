// ❌ compiler error
// LinkedList.fol
forall(T)
_(T) co.struct = { value T; next LinkedList; }

// ✅ correct
// LinkedList.fol
@co.dap.generic(types=[{name=T}])
_ co.struct = { value T; next LinkedList; }

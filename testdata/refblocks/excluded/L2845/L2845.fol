//Monoid.fol
@co.dap.typeclass(kind=Monoid, shape=(T))
_ co.typeclass = {
    empty() -> (T);
    combine(a T, b T) -> (T);
}

// IntMonoid.fol
_ co.instance->(for=Monoid, type=co.int) = {
    empty()->(co.int) = { this => 0; }
    combine(a co.int, b co.int)->(co.int) = { this => a + b; }
}

_ co.unit = {
    doSomething(input co.int)->(co.int, co.bool) = {
        this => 20, co.const.true;
    }

    printValue(value co.int)->() = {
        (value < 0).then({ this ^=>; });
        co.out.println(value);
    }
}

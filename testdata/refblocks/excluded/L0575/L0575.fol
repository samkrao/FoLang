// Limits.fol
_ co.struct = {
    minimum positiveInt;
}

// limits.unit.fol
_ co.unit = {
    demonstrate()->() = {
        valid Limits = Limits{minimum: 1}; // valid
        invalid Limits = Limits{};         // compile-time error: Uninitialized
    }
}

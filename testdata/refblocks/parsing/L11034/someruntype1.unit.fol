_ co.unit = {
    selectValue(value co.int)->(co.tag) = {
        (value < 100)
            .then(co.tag(co.string, "Hello"))
            .default(
                co.tag(co.bool, co.const.true)
            );
    }
}

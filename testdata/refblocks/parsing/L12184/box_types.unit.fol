// box_types.unit.fol
_ co.unit = {
    IntList co.type = co.List(co.int);
    somFun ()->()={
    	value IntList;
    }

    // parameterized co.type
    Option(T) co.type = co.variants(Some(T), None);
    someOtherFun()->()={
    	value Option(co.int);
	}
}


_ co.unit= {

    inspect(t someType)->() = {
        (t == co.int).then({
            // Handle the int type object.
        }).otherwise(t == co.string).then({
            // Handle the string type object.
        });
    }

}

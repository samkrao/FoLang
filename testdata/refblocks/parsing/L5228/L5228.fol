// valid named payload declarations
_ co.enum = {
    Move(x co.int, y co.int),
    Text(body co.string),
    Quit
}

// invalid model: enum payload components cannot be positional/type-only
// Move(co.int, co.int)

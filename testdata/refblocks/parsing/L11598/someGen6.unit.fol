_ co.lang.unit = {
    @co.dap.generic(
        types=[{name=T}],
        aliases=[{name=Adder, type=(T)->(T)}]
    )
    makeAdder(a T)->(Adder) = {
        adder Adder = (b T)->(T) {
            this => a + b;
        };
        this => adder;
    }
}

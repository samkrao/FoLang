// scope_example.unit.fol
_ co.unit = {

    foo()->() = {
        x co.int = 10;

        printX()->() = {
            co.out.println(x);  // ✅ x from the enclosing lexical scope
        }

        printX();
    }
}

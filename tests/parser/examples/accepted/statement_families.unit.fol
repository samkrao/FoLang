_ co.unit = {
    holder()->() = {
        typed co.int = 1, other co.int = 2;
        inferred := 3, reusable ?= 4;
        (left co.int, right co.int);
        left, right = right, left;
        emptyCall();
        ;
        'label: { localValue := 1; }
        {
            nested := 2;
            nested
        }
    }
}

_ co.lang.unit = {
    plain()->(co.lang.string) = { this.return "x"; }

    @co.dap.public
    (emp ReceiverEmployee) fullLabel()->(co.lang.string) = { this.return emp.name; }

    (ReceiverEmployee) bareReceiver()->(co.lang.int) = { this.return 0; }

    genericResult()->(Matrix(r, c)) = { this.return 0; }

    kindParam(T co.lang.type, target co.lang.function)->(co.lang.int) = { this.return 0; }
}

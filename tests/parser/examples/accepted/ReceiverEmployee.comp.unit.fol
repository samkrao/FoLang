_ co.unit = {
    plain()->(co.string) = { this => "x"; }

    @co.dap.public
    (emp ReceiverEmployee) fullLabel()->(co.string) = { this => emp.name; }

    (ReceiverEmployee) bareReceiver()->(co.int) = { this => 0; }

    genericResult()->(Matrix(r, c)) = { this => 0; }

    kindParam(T co.type, target co.function)->(co.int) = { this => 0; }
}

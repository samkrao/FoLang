// A trait is interface-like but may provide default function implementations,
// and it carries no instance state.
_ co.trait = {

    @co.dap.abstract
    someFunction()->();

    somerealFun()->() = {
        co.out.println("Doing real work");
    }
}

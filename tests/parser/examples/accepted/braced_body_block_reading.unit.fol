_ co.unit = {
    run(values co.int)->() = {
        'outer: {
            inner co.int = 1;
        }

        {
            bare co.int = 2;
        }

        values.each(index, value, {
            'label: {
                shadowed co.int = 3;
            }
        });
    }
}

@co.dap.lazy
let literalPattern(0) = 0;
let bindingPattern(value).where(value > 0) = value;
let constructorPattern(Some(value)) = value;
let recordPattern(Employee{id: value, name: _}) = value;
let tuplePattern((left, right)) = left;
offset := 1;
let captured(value) = value + offset;

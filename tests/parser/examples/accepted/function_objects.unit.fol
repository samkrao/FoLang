_ co.unit = {
    someFArg co.function = (a co.int, b co.int)->(co.int) {
        this => a + b;
    };

    add (a co.int, b co.int)->(co.int) = { this => a + b; }

    oObj co.function = add;

    @co.dap.delegate
    someDelegate co.delegate = (a co.int, b co.int)->(co.int, co.int);

    Transform co.delegate = (co.int)->(co.string);
}

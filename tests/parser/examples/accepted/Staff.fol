_ co.class = {
    id co.int;

    @co.dap.operator(symbol='+', mode=overload)
    addInstance(other Staff)->(Staff) = {
        this => other;
    }

    @co.dap.operator(symbol='-', mode=overload)
    @co.dap.static
    subtract(left Staff, right Staff)->(Staff) = {
        this => left;
    }
}

_ co.unit = {
    local(a co.int)->(co.int) = {
        helper(x co.int)->(co.int) = {
            this => x;
        }
        closure = (a co.int, b co.int) ==>> a + b;
        curried = (a co.int)(b co.int) ==>> a + b;
        this => helper(a);
    }
}

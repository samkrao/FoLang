_ co.unit = {
    IntPtr2 co.type = co.int->(**);
    IntRef2 co.type = co.int->(&&);
    IntBorrow co.type = co.int->(~);
    IntMatrix co.type = co.int->([2][3]);
    IntSlice co.type = co.int->([:]);
    IntThunk co.type = co.int->(^);
    IntRegionPtr co.type = co.int->(*, kind=region, meta={});
    IntPtr co.type = co.int->(*);
    FiveInts co.type = co.int->([5]);
    EmployeeRef co.type = Employee->(&);

    derived(
        p IntPtr2, r IntRef2, h IntBorrow, a IntMatrix,
        s IntSlice, t IntThunk, attr IntRegionPtr
    )->(IntPtr, FiveInts) = {
        this => p, a;
    }

    method(emp EmployeeRef)->(co.int) = { this => 0; }
}

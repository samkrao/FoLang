// The same rule for a class body: "A class cannot physically contain named
// class, struct, enum, module, function, signature, interface, or other
// declaration definitions as nested declarations."
_ co.class = {
    Address co.struct = {
        city co.string;
    }
}

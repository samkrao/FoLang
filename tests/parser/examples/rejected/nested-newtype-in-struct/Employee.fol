// The same rule for the rest of the non-UDT family, and in a body whose members
// really are fields, so the two readings compete directly.
_ co.struct = {
    id co.int;

    Count co.newtype = co.int;
}

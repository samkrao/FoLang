_ co.unit = {
    demonstrate(source co.string, companionType co.string)->() = {
        tokenizer := Tokenizer{
            source: source,
            position: 0,
            line: 1,
            column: 1,
            tokens:TokenList{}
        };
        tokens := tokenizer.tokenize();
        parser := Parser{
            tokens: tokens,
            position: 0,
            contexts: ParseContextList{ParseContext.File},
            companionType: companionType
        };
        tree := parser.parseProject();
        parser.printNode(tree);
    }
}

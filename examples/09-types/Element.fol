// Element.fol — package source file
// Spec: language-ref.md > Builtin Kinds
// Grammar: type-declaration-kind
//
// `co.lang.associatedtype` is one of the type-declaration kinds. Unlike a
// plain alias it is not on the entry file's allowed list, so it belongs in a
// package source file.

_ co.lang.signature = {
    Element co.lang.associatedtype;
}

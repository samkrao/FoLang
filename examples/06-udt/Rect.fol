// Rect.fol — package source file
// Spec: language-ref.md > CStructs
//
// A cstruct may contain other cstructs and fixed-size arrays. Everything stays
// copied by value with an exact, metadata-free layout.

_ co.lang.cstruct = {
    origin  Point;
    width   co.lang.int;
    height  co.lang.int;
    Corners co.lang.type = co.lang.int->([4]);
    corners Corners;
}
